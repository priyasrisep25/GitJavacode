package com.syntax.class05;

public class LagicOr2 {

	public static void main(String[] args) {
		// TODO Auto-generated method stub

		boolean selected=true;

		if (!selected) { //the output will come only if the conditon is true, in here by putting!Selected, 
			//the true become false so it's not executing.
			
			System.out.println("do click");
			
	}
		
		boolean Select =false;
		
		if(!Select) { //in here inside () will be true, because false changed to true by just putting !in front of the variable(Select)
			System.out.println("do click");
			
		}

}
}
